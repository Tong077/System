@extends('layout.apps')
@section('content')
    <div class="card">
        <div class="card-header">
            <h4 class="card-title text-muted">Link Management</h4>
            <a href="{{ route('links.create') }}" class="btn btn-success btn-sm float-right"><i class="fa-solid fa-plus"></i> Add New Link</a>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table">
                    <thead>
                        <tr>
                            <th>Image</th>
                            <th>Title</th>
                            <th>URL</th>
                            <th>Department</th>
                            <th>Description</th>
                            <th class="text-right">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach ($links as $link)
                            <tr>
                                <td>
                                    @if($link->image)
                                        <img src="{{ asset('storage/' . $link->image) }}" alt="Link Image" style="width: 40px; height: 40px; object-fit: cover; border-radius: 8px;">
                                    @else
                                        <div style="width: 40px; height: 40px; background: #f0f0f0; border-radius: 8px; display: flex; align-items: center; justify-content: center; color: #ccc; font-size: 10px;">N/A</div>
                                    @endif
                                </td>
                                <td>{{ $link->title }}</td>
                                <td><a href="{{ $link->url }}" target="_blank">{{ $link->url }}</a></td>
                                <td>{{ $link->department }}</td>
                                <td>{{ $link->description }}</td>
                                <td class="text-right">
                                    <a href="{{ route('links.edit', $link->id) }}" class="btn btn-sm btn-primary">Edit</a>
                                    <form action="{{ route('links.destroy', $link->id) }}" method="POST" class="d-inline delete-form">
                                        @csrf
                                        @method('DELETE')
                                        <button type="submit" class="btn btn-sm btn-danger">Delete</button>
                                    </form>
                                </td>
                            </tr>
                        @endforeach
                    </tbody>
                </table>
                {{ $links->links() }}
            </div>
        </div>
    </div>
@endsection
